# NormDorm — Environment Variables
# Copy to .env and fill in your values, or add to Vercel project settings.

# Upstash Redis KV (global leaderboard)
# Create a free Redis database at https://upstash.com
KV_REST_API_URL=https://YOUR_UPSTASH_URL.upstash.io
KV_REST_API_TOKEN=your_upstash_rest_token
